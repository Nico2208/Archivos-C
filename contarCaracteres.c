#include <stdio.h>

/*counts characters in input: 2nd version*/ 
main(){
    double nc;
    for(nc = 0; getchar() != EOF; ++nc){ //Todo el trabajo del for se realiza en el tester y en el incremento
        ; //Null statement
    }
    printf("%.0f\n", nc);
}